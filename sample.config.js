// rename this file to config.js

var config = {};

// path of where the retrieved git repo should be written
config.projectDir = "~/projects/mycdproject";
config.startupScript = "main.py";

module.exports = config;